<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:FirstMauiApp.Pages"
             x:Class="FirstMauiApp.Pages.RoomsPage"
             Title="RoomsPage">

    <VerticalStackLayout
        Padding="30,0"
        Spacing="25">

        <Label 
            Text="Your Rooms"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

<!--
-->
        <ListView BackgroundColor="Azure" >
            <ListView.ItemsSource>
              <x:Array Type="{x:Type x:String}">
                  <x:String>Кухня</x:String>
                  <x:String>Ванная</x:String>
                  <x:String>Гостиная</x:String>
                  <x:String>Детская</x:String>
                  <x:String>Прихожая</x:String>
              </x:Array>
            </ListView.ItemsSource>
        </ListView>

<!--
        <CollectionView>
            <CollectionView.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>Item 1</x:String>
                    <x:String>Item 2</x:String>
                    <x:String>Item 3</x:String>
                </x:Array>
            </CollectionView.ItemsSource>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
-->

        <Button
            x:Name="addBtn"
            Text="{x:Static pages:RoomsPage.BUTTON_TEXT}"
            SemanticProperties.Hint="Counts the number of times you click"
            Clicked="OnAddClicked"
            HorizontalOptions="Fill" />

    </VerticalStackLayout>

</ContentPage>
