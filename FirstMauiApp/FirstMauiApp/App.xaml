<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FirstMauiApp"
             xmlns:userdict="clr-namespace:FirstMauiApp.Resources.UserDictionary"
             x:Class="FirstMauiApp.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />

<!--
// добавленные пользователем записи словаря ресурсов
// используем ресурсы уровня приложения
<ResourceDictionary></ResourceDictionary>
<userdict:MessageTextStyle />
//-
                <ResourceDictionary>
                    <Color x:Key="errorColor">#FF0000</Color>
                    <x:Double x:Key="errorFont">16</x:Double>
                </ResourceDictionary>
-->
                <userdict:MessageTextStyle />

<!--
// добавленные пользователем стили
-->
                <ResourceDictionary>
                    <Style x:Key="buttonStyle" TargetType="Button">
                        <Setter Property="CornerRadius" Value="15" />
                        <Setter Property="Opacity" Value="0.8" />
                        <Style.Triggers>
                            <!-- триггер, который будет отслеживать нажатие на кнопку
                             и менять цвет текста на ней -->
                            <Trigger Property="IsPressed" Value="True" TargetType="Button">
                                <Setter Property="TextColor" Value="LightBlue" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>

<!--
// добавленные пользователем визуальные состояния
-->
         <Style TargetType="Entry" x:Key="ValidInputStyle">
            <Setter Property="VisualStateManager.VisualStateGroups">
               <VisualStateGroupList>
                  <VisualStateGroup x:Name="ValidStates">
                     <VisualState x:Name="Valid">
                        <VisualState.Setters>
                           <Setter Property="TextColor" Value="CornflowerBlue" />
                        </VisualState.Setters>
                     </VisualState>
                     <VisualState x:Name="Invalid">
                        <VisualState.Setters>
                           <Setter Property="TextColor" Value="IndianRed" />
                        </VisualState.Setters>
                     </VisualState>
                     <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                           <Setter Property="BackgroundColor" Value="LightCyan" />
                        </VisualState.Setters>
                     </VisualState>
                  </VisualStateGroup>
               </VisualStateGroupList>
            </Setter>
         </Style>

                </ResourceDictionary>

            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
